

activity_dictionary = {
    "15° trunk extension": 1,
    "Neutral standing": 2,
    "30° trunk flexion (arms hanging down)": 3,
    "45° trunk flexion (arms hanging down)": 4,
    "90° trunk flexion (arms hanging down)": 5,
    "Standing with 10 kg weights in each hand at the side": 6,
    "Standing with 20 kg weight in left hand at the side": 7,
    "Standing with 20 kg weights in each hand at the side": 8,
    "Standing with 10 kg weights in each hand, elbows flexed to 90°": 9,
    "30° trunk flexion with 10 kg weights in each hand": 10,
    "90° trunk flexion with 10 kg weights in each hand": 11,
    "30° trunk axial twist to the left": 12,
    "20° trunk lateral bend to the right": 13,
    "Trunk axial twist and trunk flexion, each at 30° with 10 kg weight": 14,
    "30° trunk axial twist, 10 Kg weight in each hand, elbows flexed to 90": 15,
    "20° trunk lateral bend to the right, with 20 kg in right hand": 16,
    "Standing with 20 kg weight in right hand": 17,
    "75° of trunk flexion": 18,
    "75° of trunk flexion with 10 kg weights in each hand": 19,
    "Opening window with both hands": 20,
    "Opening window with one hand": 21,
    "Push shopping cart": 22,
    "Opening door, Pull": 23,
    "Opening door, Push": 24,
    "Tie shoes: 90° of flexion all in lumbar spine, pelvis upright": 25,
    "Standing with 5 kg in each hand": 26,
    "10° trunk flexion": 27,
    "10° trunk flexion, 5 kg in each hand": 28,
    "20° trunk flexion": 29,
    "20° trunk flexion, 5 kg in each hand": 30,
    "30° trunk flexion, 5 kg in each hand": 31,
    "Shoulder flexion 90°, right arm": 32,
    "Shoulder flexion 90°, both arms": 33,
    "Shoulder abduction 90°, both arms": 34,
    "Standing with arms in, holding 4kg in each hand": 35,
    "Standing with arms out": 36,
    "Standing with arms out, holding 4kg in each hand": 37,
    "30° of trunk flexion with arms out": 38,
    "30° of trunk flexion with arms out, holding 4 kg in each hand": 39,
    "5° trunk extension": 40,
    "30° trunk extension": 41,
    "20° shoulder flexion 5 kg in each hand": 42,
    "40° shoulder flexion 5 kg in each hand": 43,
    "60° shoulder flexion 5 kg in each hand": 44,
    "80° shoulder flexion 5 kg in each hand": 45,
    "20° shoulder flexion 5 kg in right hand": 46,
    "40° shoulder flexion 5 kg in right hand": 47,
    "60° shoulder flexion 5 kg in right hand": 48,
    "80° shoulder flexion 5 kg in right hand": 49,
    "20° shoulder abduction 5 kg in each hand": 50,
    "40° shoulder abduction 5 kg in each hand": 51,
    "60° shoulder abduction 5 kg in each hand": 52,
    "80° shoulder abduction 5 kg in each hand": 53,
    "20° shoulder abduction 5 kg in right hand": 54,
    "40° shoulder abduction 5 kg in right hand": 55,
    "60° shoulder abduction 5 kg in right hand": 56,
    "80° shoulder abduction 5 kg in right hand": 57,
    "0° trunk flexion 5 kg in each hand": 58,
    "20° trunk flexion 5 kg in each hand": 59,
    "40° trunk flexion 5 kg in each hand": 60,
    "60° trunk flexion 5 kg in each hand": 61,
    "0° trunk flexion 5 kg in right hand": 62,
    "20° trunk flexion 5 kg in right hand": 63,
    "40° trunk flexion 5 kg in right hand": 64,
    "60° trunk flexion 5 kg in right hand": 65,
    "0° trunk lateral bend 5 kg in right hand": 66,
    "10° trunk lateral bend 5 kg in right hand": 67,
    "20° trunk lateral bend 5 kg in right hand": 68,
    "30° trunk lateral bend 5 kg in right hand": 69,
    "30° trunk axial twist, 0° flexion, 5 kg in each hand": 70,
    "30° trunk axial twist, 20° flexion, 5 kg in each hand": 71,
    "30° trunk axial twist, 40° flexion, 5 kg in each hand": 72,
    "30° trunk axial twist, 60° flexion 5 kg in each hand": 73,
    "30° trunk axial twist, 0° flexion, 5 kg in right hand": 74,
    "30° trunk axial twist, 20° flexion, 5 kg in right hand": 75,
    "30° trunk axial twist, 40° flexion, 5 kg in right hand": 76,
    "30° trunk axial twist, 60° flexion, 5 kg in right hand": 77,
    "0° shoulder flexion, 90° elbow flexion, 5 kg in each hand": 78,
    "45° shoulder flexion, 90° elbow flexion, 5 kg in each hand": 79,
    "90° shoulder flexion, 90° elbow flexion, 5 kg in each hand": 80,
    "0° shoulder flexion, 90° elbow flexion, 5 kg in right hand": 81,
    "45° shoulder flexion, 90° elbow flexion, 5 kg in right hand": 82,
    "90° shoulder flexion, 90° elbow flexion, 5 kg in right hand": 83,
    "0° trunk axial twist, 5 kg in each hand": 84,
    "15° trunk axial twist, 5 kg in each hand": 85,
    "30° trunk axial twist, 5 kg in each hand": 86,
    "45° trunk axial twist, 5 kg in each hand": 87,
    "0° trunk axial twist, 5 kg in right hand": 88,
    "15° trunk axial twist, 5 kg in right hand": 89,
    "30° trunk axial twist, 5 kg in right hand": 90,
    "45° trunk axial twist, 5 kg in right hand": 91,
    "0° shoulder flexion, 90° elbow flexion, 5 kg in each hand": 92,
    "45° shoulder flexion, 45° elbow flexion, 5 kg in each hand": 93,
    "90° shoulder flexion, 0° elbow flexion, 5 kg in each hand": 94,
    "0° shoulder flexion, 90° elbow flexion, 5 kg in right hand": 95,
    "45° shoulder flexion, 45° elbow flexion, 5 kg in right hand": 96,
    "90° shoulder flexion, 0° elbow flexion, 5 kg in right hand": 97,
    "Wearing a backpack (standing)": 98,
    "Wearing a backpack (30° trunk flexion)": 99,
    "Wearing a backpack (20° trunk lateral bend)": 100,
    "Carrying something on one shoulder (standing)": 101,
    "Pushing forward, elbows flexed 90°": 102,
    "Pulling back, elbows flexed 90°": 103,
    "Pushing down, elbows flexed 90°": 104,
    "Pushing forward, arms at side": 105,
    "Pulling back, arms at side": 106,
    "Pushing down, arms at side": 107,
    "Pushing forward, shoulders flexed 30°": 108,
    "Pulling back, shoulders flexed 30°": 109,
    "Pushing down, shoulders flexed 30°": 110,
    "Pushing forward, shoulders flexed 130°": 111,
    "Pulling back, shoulders flexed 130°": 112,
    "Pushing down, shoulders flexed 130°": 113,
    "Pushing forward, shoulders flexed 130°_2": 114,
    "Pulling back, shoulders flexed 130°_2": 115,
    "Pushing down, shoulders flexed 130°_2": 116,
    "Pushing to the side, shoulders flexed 130°": 117,
    "Sit-up: start by lying flat on floor": 118,
    "Sit-up: 25° flexion at pelvis": 119,
    "Sit-up: 50° flexion at pelvis": 120,
    "Shoveling - Upright": 121,
    "Shoveling - 30° trunk flexion": 122,
    "Shoveling - 60° trunk flexion": 123,
    "Shoveling - Upright and 30° axial rotation": 124,
    "Shoveling - 30° trunk flexion and 30° axial rotation": 125,
    "Shoveling - 60° trunk flexion and 30° axial rotation": 126
}



#%% 

import os
import json
import subprocess
import sys
import io
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import google.generativeai as genai
from openai import OpenAI

# --- MISSING IMPORTS CAUSING YOUR ERROR ---
from langchain_openai import ChatOpenAI
from langchain_google_genai import ChatGoogleGenerativeAI
from langchain_experimental.agents import create_pandas_dataframe_agent

# MODEL = 'gemini'
# MODEL_TYPE = 'gemini-2.5-flash-lite'
# MODEL_TYPE = 'gemini-2.5-flash'


MODEL = 'openai'
# MODEL_TYPE = 'gpt-4o-mini'
MODEL_TYPE = 'gpt-5-nano'
# MODEL_TYPE = 'gpt-5.2'


def run_opensim_simulation(
        model_path, 
        motion_path, 
        external_force_path):
    cwd = os.getcwd()
    os.chdir(os.path.join(cwd, 'opensim_files'))

    model_file  = os.path.abspath(model_path)
    motion_file = os.path.abspath(
        os.path.join('StaticActivities_126', 'Motions 126', motion_path)
    )

    forces_file = os.path.abspath(
        os.path.join('StaticActivities_126', 'External Forces 126', external_force_path)
    )

    # 2. Build the command as a SINGLE STRING
    # We wrap paths in quotes "{variable}" to handle spaces safely
    cmd = f'conda run -n opensim --no-capture-output python opensim_run.py "{model_file}" "{motion_file}" "{forces_file}"'

    print("Running OpenSim via conda...")
    print(f"Command: {cmd}")

    try:
        # 3. Execute and capture output
        # When cmd is a string, we usually need shell=True
        result = subprocess.run(cmd, capture_output=True, text=True, shell=True)

        # Check if the command actually worked (return code 0)
        if result.returncode != 0:
            raise subprocess.CalledProcessError(result.returncode, cmd, output=result.stdout, stderr=result.stderr)

        # 4. Parse JSON
        output_str = result.stdout
        json_start_index = output_str.find('{')
        
        os.chdir(cwd)

        if json_start_index != -1:
            json_data = output_str[json_start_index:]
            data = json.loads(json_data)

            # 5. Get your variables
            spinal_loads = data.get("spinal_loads")
            muscle_forces = data.get("muscle_forces")
            muscle_activations = data.get("muscle_activations")

            print("Success!")
            # print(f"Spinal Loads keys: {list(spinal_loads.keys()) if spinal_loads else 'None'}")
            
            return spinal_loads, muscle_forces, muscle_activations

        else:
            print("Error: No JSON data found in output.")
            print("Raw Output:\n", output_str)
        
        os.chdir(cwd)

    except subprocess.CalledProcessError as e:
        print("Error: The OpenSim process failed.")
        print("Error Log:", e.stderr)
        
        os.chdir(cwd)
        return None, None, None
    
    except json.JSONDecodeError as e:
        print("Error: Failed to decode the JSON response.")
        print("Raw Output:", result.stdout)
        
        os.chdir(cwd)
        return None, None, None




def analyze_request_node(state):
    """
    Node 1: The 'Brain'. Extracts a LIST of activities and Subject Filters.
    Compatible with both OpenAI and Gemini.
    """
    print(f"--- Node 1: Analyzing Request ({MODEL}) ---")
    
    # 1. Load Keys (Load both just in case, or specific one)
    try:
        with open('info_and_keys.json') as f:
            keys = json.load(f)
    except Exception as e:
        return {"final_message": f"Config Error: {e}"}

    # 2. Build Prompt (SHARED for both models)
    prompt = f"""
    You are an OpenSim expert. Analyze: "{state['user_prompt']}"

    ### TASK 1: Subject Filter
    - Extract constraints for Sex, Age, Weight, Height.
    - Output standardized ranges [min, max]. 
    - If "single subject", min and max are equal. 
    - If "batch/group", use the range defined (use -1 if undefined).

    ### TASK 2: Activity Matching
    - Identify ALL activities requested by the user.
    - Match them exactly to strings in this list: {list(activity_dictionary.keys())}
    - If the user implies a category (e.g., "all lifting"), select all relevant keys.

    ### Height should be in meter and weight in kilogram, so convert if needed. 

    ### OUTPUT JSON ONLY:
    {{
      "is_relevant": true,
      "subject_filter": {{
          "sex": "male" | "female" | "any",
          "age_range": [min, max],
          "weight_range": [min, max],
          "height_range": [min, max]
      }},
      "activity_keys": ["Activity Name 1", "Activity Name 2"],
      "verification": "Simulating [User's Subject] performing [Activity Count] tasks..."
    }}
    """

    # 3. Call the Selected API
    try:
        content = ""

        if MODEL == 'openai':
            client = OpenAI(api_key=keys['openai_api_key'])
            response = client.chat.completions.create(
                model=MODEL_TYPE,
                messages=[
                    {"role": "system", "content": "You are a data extraction assistant. Output strictly valid JSON."},
                    {"role": "user", "content": prompt}
                ],
                response_format={"type": "json_object"}
            )
            content = response.choices[0].message.content

        elif MODEL == 'gemini':
            genai.configure(api_key=keys['gemini_api_key'])
            model = genai.GenerativeModel(MODEL_TYPE)
            response = model.generate_content(prompt)
            # Gemini often adds markdown backticks, so we strip them
            content = response.text.replace('```json', '').replace('```', '').strip()
            
        else:
            return {"final_message": f"Error: Unknown MODEL configuration '{MODEL}'"}

        # 4. Parse JSON (Common Step)
        result = json.loads(content)
        
        msg = f"Analyzed Request: {result.get('verification', 'Processing...')}"
        return {"analysis_result": result, "current_status": msg}

    except Exception as e:
        print(f"API Error ({MODEL}): {e}")
        return {"analysis_result": {"is_relevant": False}, "final_message": f"AI Error: {e}"}




def model_selection_node(state):
    """
    Node 2: The 'Selector'. 
    1. Tries to find models using strict Python logic.
    2. If 0 found, asks LLM to adjust criteria based on DB stats.
    3. Retries up to 3 times.
    """
    print(f"--- Node 2: Selecting Model ({MODEL}) ---")
    
    # 1. Setup Data & Config
    analysis = state.get('analysis_result')
    if not analysis or 'subject_filter' not in analysis:
        return {"final_message": "Error: Missing subject analysis."}
    
    # Deep copy filters to allow modification
    current_filters = {
        'sex': analysis['subject_filter']['sex'],
        'age_range': list(analysis['subject_filter']['age_range']),
        'weight_range': list(analysis['subject_filter']['weight_range']),
        'height_range': list(analysis['subject_filter']['height_range'])
    }

    # Load Database
    try:
        csv_path = os.path.join(os.getcwd(), 'opensim_files', 'ms_model_details.csv')
        df = pd.read_csv(csv_path)
        df.columns = df.columns.str.strip()
    except Exception as e:
        return {"final_message": f"Error reading CSV: {e}"}

    # --- HELPER: Pure Python Filtering Logic ---
    def apply_filters(df_in, filters_in):
        # 1. Sex Filter
        if filters_in['sex'] != 'any':
            df_out = df_in[df_in['sex'].str.lower() == filters_in['sex'].lower()]
        else:
            df_out = df_in.copy()
            
        if df_out.empty: return df_out

        # 2. Check Mode
        # If min != max for any metric, we treat it as a "Range/Batch" request
        is_batch = (filters_in['age_range'][0] != filters_in['age_range'][1] or 
                    filters_in['weight_range'][0] != filters_in['weight_range'][1] or 
                    filters_in['height_range'][0] != filters_in['height_range'][1])
        
        if is_batch:
            mask = pd.Series(True, index=df_out.index)
            # Age
            mn, mx = filters_in['age_range']
            if mn != -1: mask &= (df_out['Min Age (year)'] <= mx) & (df_out['Max Age (year)'] >= mn)
            # Weight
            mn, mx = filters_in['weight_range']
            if mn != -1: mask &= (df_out['weight (kg)'] >= mn) & (df_out['weight (kg)'] <= mx)
            # Height
            mn, mx = filters_in['height_range']
            if mn != -1: mask &= (df_out['height (m)'] >= mn) & (df_out['height (m)'] <= mx)
            return df_out[mask]
        
        else:
            # Single Best Match Logic (Distance Metric)
            target_a, target_w, target_h = filters_in['age_range'][0], filters_in['weight_range'][0], filters_in['height_range'][0]
            scores = np.zeros(len(df_out))
            
            if target_a != -1:
                mid = (df_out['Min Age (year)'].values + df_out['Max Age (year)'].values) / 2
                scores += np.abs(mid - target_a) / 10.0
            if target_w != -1: scores += np.abs(df_out['weight (kg)'].values - target_w) / 10.0
            if target_h != -1: scores += np.abs(df_out['height (m)'].values - target_h) / 0.10
            
            best_idx = scores.argmin()
            return df_out.iloc[[best_idx]]

    # --- REFLECTION LOOP ---
    max_retries = 3
    selected_df = pd.DataFrame()

    # Load API Keys once
    try:
        with open('info_and_keys.json') as f: keys = json.load(f)
        if MODEL == 'openai': client = OpenAI(api_key=keys['openai_api_key'])
        elif MODEL == 'gemini': genai.configure(api_key=keys['gemini_api_key'])
    except:
        return {"final_message": "Error: API Keys missing."}

    for attempt in range(1, max_retries + 1):
        print(f"-> Attempt {attempt}: Filtering...")
        
        # A. Try Python Logic
        selected_df = apply_filters(df, current_filters)
        
        if not selected_df.empty:
            print(f"   -> Success: Found {len(selected_df)} models.")
            break
        
        # B. IF FAILED: Ask LLM to adjust parameters
        if attempt < max_retries:
            print("   -> No models found. Asking LLM to adjust filters...")
            
            # Calculate DB Stats for Context (filtered by sex first)
            sex_filter = current_filters['sex']
            if sex_filter != 'any':
                context_df = df[df['sex'].str.lower() == sex_filter.lower()]
            else:
                context_df = df
            
            if context_df.empty:
                return {"final_message": f"Error: No models exist for sex='{sex_filter}'."}

            stats = {
                "min_age": float(context_df['Min Age (year)'].min()),
                "max_age": float(context_df['Max Age (year)'].max()),
                "min_weight": float(context_df['weight (kg)'].min()),
                "max_weight": float(context_df['weight (kg)'].max()),
                "min_height": float(context_df['height (m)'].min()),
                "max_height": float(context_df['height (m)'].max())
            }

            prompt = f"""
            You are a Data Logic Assistant.
            GOAL: Adjust the search filters to find at least one model.
            
            CURRENT FILTERS (Failed):
            {json.dumps(current_filters, indent=2)}
            
            DATABASE STATS (Actual Data Range):
            {json.dumps(stats, indent=2)}
            
            TASK:
            Relax the 'age_range', 'weight_range', or 'height_range' so they overlap with the Database Stats.
            - If the user wanted [70, 70] but data is 60-80, widen to [65, 75].
            - If the user request is completely out of bounds, shift it towards the min/max of the DB.
            - Keep 'sex' unchanged.
            
            OUTPUT:
            Return ONLY a valid JSON object with the updated filters. 
            Example: {{"sex": "male", "age_range": [20, 30], "weight_range": [70, 80], "height_range": [1.7, 1.8]}}
            """

            # Call LLM
            try:
                content = ""
                if MODEL == 'openai':
                    response = client.chat.completions.create(
                        model="gpt-4o-mini",
                        messages=[{"role": "user", "content": prompt}],
                        response_format={"type": "json_object"},
                        temperature=0
                    )
                    content = response.choices[0].message.content
                elif MODEL == 'gemini':
                    model = genai.GenerativeModel("gemini-2.0-flash-exp")
                    response = model.generate_content(prompt)
                    content = response.text.replace('```json', '').replace('```', '').strip()
                
                # Apply new filters
                new_filters = json.loads(content)
                current_filters.update(new_filters)
                print(f"   -> LLM Adjusted Filters: {current_filters}")
                
            except Exception as e:
                print(f"   -> LLM Error: {e}")
                break

    # --- FINAL OUTPUT ---
    if selected_df.empty:
        return {"final_message": "Error: No models found matching criteria after 3 attempts."}

    results = []
    for _, row in selected_df.iterrows():
        raw_dir = row['Directory']
        if pd.isna(raw_dir): continue
        clean_path = f"{str(raw_dir).replace('\\', '/').lstrip('./')}/{row['Filename']}"
        results.append({**row.to_dict(), "full_path": clean_path})

    msg = f"Model Selection Complete. Found {len(results)} model(s)."
    return {"selected_models": results, "current_status": msg}




def simulation_node(state):
    """
    Node 3: The 'Action'. Loops through selected Models and Activities 
    to run OpenSim simulations for every combination.
    """
    print("--- Node 3: Running Simulations ---")
    
    # 1. Retrieve Inputs from State
    models = state.get('selected_models', [])
    activity_names = state['analysis_result'].get('activity_keys', [])
    
    # 2. Load Activity ID Dictionary (from your json file)
    activity_map = activity_dictionary

    results_log = []

    # 3. Double Loop: Iterate over every Person AND every Activity
    for model_info in models:
        for act_name in activity_names:
            
            # Get the numeric ID (e.g., 'Neutral standing' -> 2)
            act_id = activity_map.get(act_name)
            
            if act_id is None:
                print(f"Warning: No ID found for '{act_name}'. Skipping.")
                continue

            # 4. Define Paths
            # (Node 2 already created 'full_path', but we handle fallback just in case)
            model_path = model_info.get('full_path')
            if not model_path:
                # Reconstruct if missing
                model_path = os.path.join(os.getcwd(), 'opensim_files', 
                                          model_info['Directory'], 
                                          model_info['Filename'])

            # Define Motion/Force paths based on ID
            # assuming these are in a fixed 'inputs' folder or similar
            motion_path = f"NMB_Motion{act_id}.mot" 
            force_path  = f"NMB_ExternalForce{act_id}.mot"
            
            
            print(f"-> Running: {act_name} (ID: {act_id}) on {model_info['Filename']}...")

            # 5. Run the Simulation (Mocked Placeholder)
            try:
                # ---------------------------------------------------------
                # REAL CALL GOES HERE:
                spinal_loads, muscle_forces, muscle_activations = run_opensim_simulation(
                    model_path, 
                    motion_path, 
                    force_path)

                
                # ---------------------------------------------------------
                
                # Mocking success
                results_log.append({
                    "model": model_info['Filename'],
                    "activity": act_name,
                    "status": "Success",
                    "id_used": act_id,
                    "results": {
                        "spinal_loads": spinal_loads,
                        "muscle_forces": muscle_forces,
                        "muscle_activations": muscle_activations
                    }
                })
                
            except Exception as e:
                results_log.append({
                    "model": model_info['Filename'],
                    "activity": act_name,
                    "status": f"Failed: {e}",
                    "results": None
                })

    # 6. Final Summary
    total_runs = len(results_log)
    success_count = sum(1 for r in results_log if r['status'] == "Success")
    
    summary_msg = (
        f"Completed {success_count}/{total_runs} simulations.\n"
        f"Models: {len(models)} | Activities: {len(activity_names)}"
    )
    
    return {
        "simulation_output": results_log, 
        "final_message": summary_msg,
        "current_status": f"Simulation Finished. {summary_msg}"
    }



# = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =
# = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =


def data_processing_node(state):
    """
    Node 4: Processes raw simulation results into structured DataFrames.
    """
    print("--- Node 4: Processing Results to DataFrames ---")
    
    sim_outputs = state.get('simulation_output', [])
    models = state.get('selected_models', [])
    
    # 1. Create a lookup for model demographics
    model_map = {
        m['Filename']: {
            'age': (m.get('Min Age (year)', 0) + m.get('Max Age (year)', 0)) / 2, 
            'weight': m.get('weight (kg)', 0),
            'height': m.get('height (m)', 0)
        } 
        for m in models
    }

    # 2. Lists to hold rows
    spinal_data, force_data, activation_data = [], [], []

    # 3. Helper to process dictionary to list of rows
    def extract_rows(base_info, data_dict, category_col, target_list):
        if not data_dict: return
        for key, value in data_dict.items():
            row = base_info.copy()
            row[category_col] = key 
            row['Value'] = value
            target_list.append(row)

    # 4. Loop through results
    for run in sim_outputs:
        if run.get('status') != 'Success': continue
            
        model_name = run['model']
        demos = model_map.get(model_name, {})
        
        base_row = {
            'Model': model_name,
            'Activity': run['activity'],
            'Age': demos.get('age'),
            'Weight_kg': demos.get('weight'),
            'Height_m': demos.get('height')
        }

        results = run.get('results', {})
        extract_rows(base_row, results.get('spinal_loads'), 'Load_Name', spinal_data)
        extract_rows(base_row, results.get('muscle_forces'), 'Muscle_Name', force_data)
        extract_rows(base_row, results.get('muscle_activations'), 'Muscle_Name', activation_data)

    # 5. Return DataFrames in state
    return {
        "dataframes": {
            "spinal": pd.DataFrame(spinal_data),
            "forces": pd.DataFrame(force_data),
            "activations": pd.DataFrame(activation_data)
        },
        "current_status": "Data processed into DataFrames."
    }



# def analysis_agent_node(state):
#     """
#     Node 5: The 'Analyst'. Connects to Chat logic using Pandas Dataframe Agents.
#     Integrates the specific prompt logic provided in utils.
#     """
#     print("--- Node 5: Agentic Analysis (Chat) ---")
    
#     dfs = state.get('dataframes')
#     if not dfs:
#         return {"final_message": "Error: No data available for analysis."}

#     # 1. Load Keys
#     try:
#         with open('info_and_keys.json') as f:
#             keys = json.load(f)
#     except:
#         return {"final_message": "Error loading keys for analysis agent."}

#     # 2. Init LLM
#     if MODEL == 'openai':
#         llm = ChatOpenAI(model=MODEL_TYPE, api_key=keys["openai_api_key"])
#     else:
#         llm = ChatGoogleGenerativeAI(model="gemini-2.0-flash-exp", google_api_key=keys["gemini_api_key"])

#     # 3. Define Domain Knowledge (Prefixes)
#     domain_knowledge = """
#     You are a Biomechanics Assistant analyzing OpenSim data.
    
#     DATA DEFINITIONS:
#     1. SPINAL LOADS:
#        - *_fy = Compression forces (Vertical).
#        - *_fx, *_fz = Shear forces (Horizontal).
#        - T1_T2...L5_S1 = Spinal joints.
    
#     2. MUSCLE FORCES (Newtons) & ACTIVATIONS (0.0-1.0):
#        - Suffixes: '_r'/'_R' = Right, '_l'/'_L' = Left.
#        - No suffix usually implies Right/Primary.
#        - Groups: 
#          * Psoas ('Ps_...')
#          * Iliocostalis ('IL_...')
#          * Longissimus ('LTpT_...', 'LTpL_...')
#          * Quadratus Lumborum ('QL_...')
#          * Multifidus ('MF_...')
#          * Abdominals ('rect_abd', 'IO', 'EO')
#     """

#     # 4. Create Agent 
#     # We allow the agent to see all 3 DataFrames to cross-reference if needed
#     agent = create_pandas_dataframe_agent(
#         llm=llm,
#         df=[dfs['spinal'], dfs['forces'], dfs['activations']],
#         verbose=True,
#         allow_dangerous_code=True, # Required for Pandas Agent
#         agent_type="openai-tools",
#         handle_parsing_errors=True,
#         prefix=domain_knowledge
#     )

#     # 5. Construct Query
#     # We combine the User's original intent + a request for summary

#     plot_instructions = """
#     Only if asked for plotting; otherwise, ignore: RULES FOR PLOTTING (MUST FOLLOW STICTLY):
#     1. SETUP:
#        - You MUST use `plt.switch_backend('Agg')` at the start of your code to prevent GUI errors.
#        - Use `plt.figure(figsize=(10, 6))` for legibility.
#        - Use `plt.style.use('ggplot')` for professional formatting.
       
#     2. DATA SELECTION:
#        - Always verify the exact column name exists in `df.columns` before plotting. Use fuzzy matching if needed (e.g., if user asks for "L5 compression", look for "L5_S1_fy").
#        - The X-Axis must ALWAYS be the 'time' column.
    
#     3. RENDERING:
#        - clear the figure with `plt.clf()` before plotting.
#        - Add a title, X-label ('Time [s]'), and Y-label (with units like 'Newtons' or 'Activation').
#        - If plotting multiple muscles, include a legend.
       
#     4. SAVING:
#        - You MUST save the file to: 'static/generated_plot.png' (Create the directory if it doesn't exist).
#        - Use `plt.savefig('static/generated_plot.png', dpi=300, bbox_inches='tight')`.
#        - DO NOT use `plt.show()`.
    
#     5. OUTPUT:
#        - After saving, your final answer MUST confirm: "Plot saved to static/generated_plot.png".
#        - Provide a brief 1-sentence interpretation of the peak values in the plot.
#     """

#     query = (
#         f"The user asked: '{state['user_prompt']}'.\n"
#         "1. Analyze the provided DataFrames (df1=Spinal, df2=Forces, df3=Activations).\n"
#         "2. If the user asks for a PLOT or GRAPH:\n"
#         f"{plot_instructions}\n"
#         "3. Provide a text summary of what the plot shows."
#     )

#     try:
#         response = agent.invoke({"input": query})
#         output_text = response["output"] if isinstance(response, dict) else str(response)
#         return {"final_message": output_text}
#     except Exception as e:
#         return {"final_message": f"Analysis Agent Error: {e}"}




# def analysis_agent_node(state):
#     """
#     Node 5: The 'Analyst'. 
#     Architecture:
#     1. PLANNER LLM: Reads history -> Decides which of the 3 DataFrames to use.
#     2. ROUTER: Activates ONLY the specific agent for that DataFrame.
#     3. SPECIALIST AGENT: executes the query with strict domain knowledge.
#     """
#     print(f"--- Node 5: Agentic Analysis ({MODEL}) ---")
    
#     dfs = state.get('dataframes')
#     if not dfs:
#         return {"final_message": "Error: No data available for analysis."}
    
#     # 1. Load Keys & Init LLM (STRICTLY USING YOUR GLOBALS)
#     try:
#         with open('info_and_keys.json') as f: 
#             keys = json.load(f)
        
#         if MODEL == 'openai':
#             llm = ChatOpenAI(
#                 model=MODEL_TYPE, 
#                 api_key=keys["openai_api_key"], 
#                 temperature=0
#             )
#         elif MODEL == 'gemini':
#             llm = ChatGoogleGenerativeAI(
#                 model=MODEL_TYPE, 
#                 google_api_key=keys["gemini_api_key"], 
#                 temperature=0
#             )
#         else:
#             return {"final_message": f"Error: Unknown MODEL configuration '{MODEL}'"}
            
#     except Exception as e:
#         return {"final_message": f"Config/LLM Init Error: {e}"}

#     # 2. Prepare Context
#     chat_history = state.get('chat_context', "No prior history.")
#     user_input = state.get('user_prompt', "")

#     # ==================================================================================
#     # STEP 1: THE PLANNER (Decide Intent & DataFrame)
#     # ==================================================================================
#     planner_prompt = f"""
#     You are the "Planner". 
    
#     CONTEXT:
#     User Input: "{user_input}"
#     Chat History: 
#     {chat_history}
    
#     AVAILABLE AGENTS / DATAFRAMES:
#     1. 'spinal': For spinal joint loads (Compression/Shear).
#        - Columns: ['Model', 'Activity', 'Load_Name', 'Value']
#     2. 'forces': For specific muscle forces (Newtons).
#        - Columns: ['Model', 'Activity', 'Muscle_Name', 'Value']
#     3. 'activations': For muscle activation levels (0.0 to 1.0).
#        - Columns: ['Model', 'Activity', 'Muscle_Name', 'Value']
    
#     YOUR TASK:
#     1. Analyze what the user is asking for.
#     2. Select ONE agent ('spinal', 'forces', or 'activations').
#     3. Write a specific instruction for that agent.
    
#     RULES:
#     - If the user asks for "L5 compression" or "Shear", choose 'spinal'.
#     - If the user asks for "Longissimus" or "Psoas" force, choose 'forces'.
#     - If the user asks for "Effort" or "Activation", choose 'activations'.
#     - If vague, default to 'spinal' with a summary request.
    
#     OUTPUT JSON ONLY:
#     {{
#         "target_agent": "spinal" | "forces" | "activations",
#         "instruction": "Precise instruction for the coding agent..."
#     }}
#     """
    
#     try:
#         response = llm.invoke(planner_prompt)
#         # Clean markdown
#         content = response.content.replace('```json', '').replace('```', '').strip()
#         plan = json.loads(content)
        
#         target = plan.get('target_agent', 'spinal')
#         instruction = plan.get('instruction', 'Summarize the data.')
        
#         print(f"[Planner] Decided: {target.upper()}")
#         print(f"[Planner] Task: {instruction}")
        
#     except Exception as e:
#         print(f"Planner Error: {e}")
#         target = "spinal"
#         instruction = "Summarize the spinal load data."

#     # ==================================================================================
#     # STEP 2 & 3: DEFINE AND SELECT THE SPECIFIC AGENT
#     # ==================================================================================
    
#     # Common Plotting Rules for all agents
#     plot_rules = """
#     IF PLOTTING:
#     - You MUST use `plt.switch_backend('Agg')` first.
#     - `plt.clf()` to clear.
#     - Save to 'static/generated_plot.png'.
#     - DO NOT use `plt.show()`.
#     """

#     selected_df = None
#     agent_prefix = ""

#     # --- AGENT A: SPINAL AGENT ---
#     if target == 'spinal':
#         selected_df = dfs['spinal']
#         agent_prefix = f"""
#         You are the SPINAL LOAD SPECIALIST.
        
#         YOUR DATA (`df`):
#         - Format: Long format (Rows are individual measurements).
#         - Columns: ['Model', 'Activity', 'Age', 'Weight_kg', 'Height_m', 'Load_Name', 'Value']
        
#         DOMAIN KNOWLEDGE:
#         - `Load_Name` contains the joint and direction.
#         - `_fy` suffix = Compression Forces (Vertical).
#         - `_fx` or `_fz` suffix = Shear Forces (Horizontal).
#         - `Value` is in Newtons.
        
#         TASK: {instruction}
        
#         TIPS:
#         - To find L5 Compression: Filter `df` where `Load_Name` contains 'L5' and 'fy'.
#         - {plot_rules}
#         """

#     # --- AGENT B: MUSCLE FORCES AGENT ---
#     elif target == 'forces':
#         selected_df = dfs['forces']
#         agent_prefix = f"""
#         You are the MUSCLE FORCE SPECIALIST.
        
#         YOUR DATA (`df`):
#         - Format: Long format.
#         - Columns: ['Model', 'Activity', 'Age', 'Weight_kg', 'Height_m', 'Muscle_Name', 'Value']
#         - `Value` is Force in Newtons.
        
#         MUSCLE NAMING CONVENTION (in `Muscle_Name`):
#         - Suffixes: '_l' or '_L' (Left), '_r' or '_R' (Right).
#         - No suffix usually implies Right.
#         - Groups (Prefixes):
#           * 'Ps_' = Psoas
#           * 'IL_' = Iliocostalis
#           * 'LTpT_', 'LTpL_' = Longissimus (Thoracic/Lumbar)
#           * 'QL_' = Quadratus Lumborum
#           * 'MF_' = Multifidus
#           * 'rect_abd', 'IO', 'EO' = Abdominals
        
#         TASK: {instruction}
        
#         TIPS:
#         - Use string contains to filter groups (e.g. `df[df['Muscle_Name'].str.contains('LTp')]`).
#         - {plot_rules}
#         """

#     # --- AGENT C: ACTIVATIONS AGENT ---
#     elif target == 'activations':
#         selected_df = dfs['activations']
#         agent_prefix = f"""
#         You are the MUSCLE ACTIVATION SPECIALIST.
        
#         YOUR DATA (`df`):
#         - Format: Long format.
#         - Columns: ['Model', 'Activity', 'Age', 'Weight_kg', 'Height_m', 'Muscle_Name', 'Value']
#         - `Value` is Activation (0.0 to 1.0). 1.0 = Max effort.
        
#         MUSCLE NAMING CONVENTION:
#         - Suffixes: '_l' (Left), '_r' (Right).
#         - Groups: 'Ps_' (Psoas), 'IL_' (Iliocostalis), 'LTpT_'/'LTpL_' (Longissimus), 'QL_' (Quad Lumb), 'MF_' (Multifidus).
        
#         TASK: {instruction}
        
#         TIPS:
#         - {plot_rules}
#         """

#     # Sanity Check
#     if selected_df is None or selected_df.empty:
#         return {"final_message": f"Error: The chosen dataframe ({target}) is empty or missing."}

#     # ==================================================================================
#     # STEP 4: EXECUTE THE CHOSEN AGENT
#     # ==================================================================================
#     try:
#         agent = create_pandas_dataframe_agent(
#             llm=llm,
#             df=selected_df,
#             verbose=True,
#             allow_dangerous_code=True,
#             agent_type="openai-tools",
#             handle_parsing_errors=True,
#             prefix=agent_prefix
#         )

#         response = agent.invoke({"input": instruction})
#         output_text = response["output"] if isinstance(response, dict) else str(response)
        
#         # Check for plot file creation hint
#         if "plot" in instruction.lower() and "saved" not in output_text.lower():
#              output_text += "\n(Note: Check the dashboard for the generated plot.)"
             
#         return {"final_message": output_text}
        
#     except Exception as e:
#         return {"final_message": f"Analysis Error in {target.upper()} agent: {e}"}



# def analysis_agent_node(state):
#     """
#     Node 5: The 'Analyst'. 
#     - USES: 3-Agent Architecture (Planner -> Specialist).
#     - FIXES: "I don't have dataframe" errors (via strict system prompts).
#     - RESTORED: Full Domain Knowledge for mapping 'Psoas' -> 'Ps_', etc.
#     """
#     print(f"--- Node 5: Agentic Analysis ({MODEL}) ---")
    
#     dfs = state.get('dataframes')
#     if not dfs:
#         return {"final_message": "Error: No data available for analysis."}
    
#     # 1. Load Keys & Init LLM
#     try:
#         with open('info_and_keys.json') as f: 
#             keys = json.load(f)
        
#         if MODEL == 'openai':
#             llm = ChatOpenAI(
#                 model=MODEL_TYPE, 
#                 api_key=keys["openai_api_key"], 
#                 temperature=0
#             )
#         elif MODEL == 'gemini':
#             llm = ChatGoogleGenerativeAI(
#                 model=MODEL_TYPE, 
#                 google_api_key=keys["gemini_api_key"], 
#                 temperature=0
#             )
#         else:
#             return {"final_message": f"Error: Unknown MODEL configuration '{MODEL}'"}
            
#     except Exception as e:
#         return {"final_message": f"Config/LLM Init Error: {e}"}

#     # 2. Context
#     chat_history = state.get('chat_context', "No prior history.")
#     user_input = state.get('user_prompt', "")

#     # ==================================================================================
#     # STEP 1: THE PLANNER
#     # ==================================================================================
#     planner_prompt = f"""
#     You are the "Planner".
    
#     USER INPUT: "{user_input}"
#     CHAT HISTORY: 
#     {chat_history}
    
#     AVAILABLE DATAFRAMES:
#     1. 'spinal' (Columns: Model, Activity, Load_Name, Value)
#     2. 'forces' (Columns: Model, Activity, Muscle_Name, Value)
#     3. 'activations' (Columns: Model, Activity, Muscle_Name, Value)
    
#     TASK:
#     1. Select ONE dataframe ('spinal', 'forces', or 'activations').
#     2. Write a Python-focused instruction.
    
#     RULES:
#     - If user asks for "L5 compression" or "Shear", use 'spinal'.
#     - If user asks for "Muscle Force" (Newtons), use 'forces'.
#     - If user asks for "Activation", "Effort" or "Recruitment" (0-1), use 'activations'.
#     - If plotting is needed, explicitly say: "Plot the data and save to static/generated_plot.png".
    
#     OUTPUT JSON ONLY:
#     {{
#         "target_agent": "spinal" | "forces" | "activations",
#         "instruction": "Exact instruction for the agent..."
#     }}
#     """
    
#     try:
#         response = llm.invoke(planner_prompt)
#         content = response.content.replace('```json', '').replace('```', '').strip()
#         plan = json.loads(content)
#         target = plan.get('target_agent', 'spinal')
#         instruction = plan.get('instruction', 'Summarize data.')
#         print(f"[Planner] Target: {target} | Instruction: {instruction}")
        
#     except Exception as e:
#         print(f"Planner Error: {e}")
#         target = "spinal"
#         instruction = "Summarize the spinal load data."

#     # ==================================================================================
#     # STEP 2: CONFIGURE THE SPECIALIST AGENT (With Full Domain Knowledge)
#     # ==================================================================================
    
#     selected_df = dfs.get(target)
#     if selected_df is None or selected_df.empty:
#         return {"final_message": f"Error: Dataframe '{target}' is empty."}

#     # --- COMMON GUARDRAILS (The Fix for "I don't have data") ---
#     base_prefix = f"""
#     You are a Python Data Analyst. 
    
#     CRITICAL EXECUTION RULES:
#     1. The dataframe is ALREADY LOADED in your environment as the variable `df`.
#     2. DO NOT ask the user for data. DO NOT say "I don't have access". USE `df` DIRECTLY.
#     3. You MUST execute Python code to answer. Do not just write the code; RUN IT.
    
#     PLOTTING RULES (IF ASKED):
#     - Use `plt.switch_backend('Agg')` at the start.
#     - `plt.clf()` to clear previous plots.
#     - Save strictly to: 'static/generated_plot.png'
#     - DO NOT use `plt.show()`.
#     """

#     # --- RESTORED DOMAIN KNOWLEDGE ---
#     if target == 'spinal':
#         system_prefix = base_prefix + """
        
#         DOMAIN KNOWLEDGE (SPINAL LOADS):
#         - `Load_Name` format: [Joint]_[Direction]
#         - COMPRESSION (Vertical): Look for suffix `_fy`. (e.g., 'L5_S1_fy')
#         - SHEAR (Horizontal): Look for suffixes `_fx` or `_fz`.
#         - JOINTS: 'T1_T2', ... 'L5_S1'.
#         - UNITS: Newtons.
        
#         TIP: To find 'L5 Compression', filter where `Load_Name` contains 'L5' AND ends with '_fy'.
#         """
        
#     elif target in ['forces', 'activations']:
#         system_prefix = base_prefix + f"""
        
#         DOMAIN KNOWLEDGE (MUSCLES):
#         - COLUMN: 'Muscle_Name'
#         - SUFFIXES: `_r` or `_R` (Right Side), `_l` or `_L` (Left Side).
#           (If no suffix is specified by user, assume Both or Right).
        
#         MUSCLE MAPPING (Prefix -> Muscle Group):
#         * 'Ps_'    -> Psoas Major
#         * 'IL_'    -> Iliocostalis (e.g., IL_L1_L2_r)
#         * 'LTpT_'  -> Longissimus Thoracis
#         * 'LTpL_'  -> Longissimus Lumborum
#         * 'QL_'    -> Quadratus Lumborum
#         * 'MF_'    -> Multifidus
#         * 'rect_abd' -> Rectus Abdominis
#         * 'IO_'    -> Internal Oblique
#         * 'EO_'    -> External Oblique
        
#         VALUE DEFINITION:
#         - If 'forces' agent: Units are Newtons.
#         - If 'activations' agent: Units are 0.0 to 1.0 (Normalized).
#         """

#     # ==================================================================================
#     # STEP 3: EXECUTE
#     # ==================================================================================
#     try:
#         agent = create_pandas_dataframe_agent(
#             llm=llm,
#             df=selected_df,
#             verbose=True,
#             allow_dangerous_code=True,
#             agent_type="openai-tools",  
#             handle_parsing_errors=True,
#             prefix=system_prefix
#         )

#         # Force the input to remind the agent AGAIN that df exists
#         final_prompt = f"{instruction} (Recall: Data is in variable `df`)"
        
#         response = agent.invoke({"input": final_prompt})
#         output_text = response["output"] if isinstance(response, dict) else str(response)

#         # Check for plot success in the text
#         if "plot" in instruction.lower() and "saved" not in output_text.lower():
#             output_text += "\n(System Note: A plot was requested. Check the dashboard.)"
            
#         return {"final_message": output_text}

#     except Exception as e:
#         return {"final_message": f"Agent Execution Error: {e}"}



def analysis_agent_node(state):
    """
    Node 5: The 'Analyst'.
    """
    print(f"--- Node 5: Agentic Analysis ({MODEL}) ---")
    
    dfs = state.get('dataframes')
    if not dfs:
        return {"final_message": "Error: No data available for analysis."}
    
    # 1. Load Keys & Init LLM
    try:
        with open('info_and_keys.json') as f: 
            keys = json.load(f)
        
        if MODEL == 'openai':
            llm = ChatOpenAI(
                model=MODEL_TYPE, 
                api_key=keys["openai_api_key"], 
                temperature=0
            )
        elif MODEL == 'gemini':
            llm = ChatGoogleGenerativeAI(
                model=MODEL_TYPE, 
                google_api_key=keys["gemini_api_key"], 
                temperature=0
            )
        else:
            return {"final_message": f"Error: Unknown MODEL configuration '{MODEL}'"}
            
    except Exception as e:
        return {"final_message": f"Config/LLM Init Error: {e}"}

    # 2. Context
    chat_history = state.get('chat_context', "No prior history.")
    user_input = state.get('user_prompt', "")

    # ==================================================================================
    # STEP 1: THE PLANNER (Decides Agent & Intent)
    # ==================================================================================
    planner_prompt = f"""
    You are the "Planner".
    
    USER INPUT: "{user_input}"
    CHAT HISTORY: 
    {chat_history}
    
    AVAILABLE AGENTS:
    1. 'spinal' (Joint Loads: Compression/Shear)
    2. 'forces' (Muscle Forces: Newtons)
    3. 'activations' (Muscle Activations: 0.0-1.0)
    
    TASK:
    1. Select ONE agent.
    2. Write a precise instruction.
    
    RULES:
    - **Plotting**: ONLY if the user explicitly asks to "plot", "graph", "visualize", or "show me", add:
      "Plot the data and save to static/generated_plot.png [with instructions on the parameters of the plot]". OTHERWISE, DO NOT PLOT.
    - **Conciseness**: Instruct the agent to keep the text response short and but accurate and actionable.
    - **Selection**:
       * "L5 compression"/"Shear" -> 'spinal'
       * "Muscle Force"/"Newtons" -> 'forces'
       * "Activation"/"Effort" -> 'activations'
    
    OUTPUT JSON ONLY:
    {{
        "target_agent": "spinal" | "forces" | "activations",
        "instruction": "Exact instruction..."
    }}
    """
    
    try:
        response = llm.invoke(planner_prompt)
        content = response.content.replace('```json', '').replace('```', '').strip()
        plan = json.loads(content)
        target = plan.get('target_agent', 'spinal')
        instruction = plan.get('instruction', 'Summarize data briefly.')
        print(f"[Planner] Target: {target} | Instruction: {instruction}")
        
    except Exception as e:
        print(f"Planner Error: {e}")
        target = "spinal"
        instruction = "Summarize the spinal load data briefly and do NOT plot."

    # ==================================================================================
    # STEP 2: CONFIGURE SHARED KNOWLEDGE & RULES
    # ==================================================================================
    
    # Shared definition for Forces & Activations
    muscle_mapping_info = """
        DOMAIN KNOWLEDGE (MUSCLES):
        - COLUMN: 'Muscle_Name'
        - SUFFIXES: `_r` or `_R` (Right Side), `_l` or `_L` (Left Side).
          (If no suffix is specified by user, assume Both or Right).
        -MUSCLE MAPPING (Prefix -> Muscle Group):
            * 'Ps_'    -> Psoas Major
            * 'IL_'    -> Iliocostalis (e.g., IL_L1_L2_r)
            * 'LTpT_'  -> Longissimus Thoracis
            * 'LTpL_'  -> Longissimus Lumborum
            * 'QL_'    -> Quadratus Lumborum
            * 'MF_'    -> Multifidus
            * 'rect_abd' -> Rectus Abdominis
            * 'IO_'    -> Internal Oblique
#           * 'EO_'    -> External Oblique
    """

    # Global Rules for all agents (Anti-Hallucination & Conciseness)
    global_rules = """
    CRITICAL EXECUTION RULES:
    1. **NO RAW CODE**: You are an analyst. DO NOT output python code blocks in the final answer. Execute the code silently.
    2. **CONCISENESS**: Keep text results SHORT, DIRECT, and NON-VERBOSE. Avoid filler words.
    3. **DATA AVAILABILITY**: The dataframe is ALREADY loaded in your environment as the variable `df`. 
       - DO NOT say "I don't have access".
       - DO NOT ask the user for data.
       - JUST USE `df`.
    4. Write python code to answer the question.
    
    PLOTTING PROTOCOL (Strict):
    - ONLY plot if specifically instructed by the prompt.
    - If plotting:
      ```python
      import matplotlib.pyplot as plt
      plt.switch_backend('Agg')
      plt.close('all')
      plt.figure(figsize=(10, 6))
      # ... plot code ...
      plt.savefig('static/generated_plot.png', bbox_inches='tight')
      ```
    - DO NOT use `plt.show()`.
    """

    selected_df = None
    system_prefix = ""

    # --- AGENT A: SPINAL SPECIALIST ---
    if target == 'spinal':
        selected_df = dfs.get('spinal')
        system_prefix = f"""
        You are the SPINAL LOAD SPECIALIST.
        {global_rules}
        
        DOMAIN KNOWLEDGE (SPINAL):
        - `Load_Name`: [Joint]_[Direction]
        - COMPRESSION (Vertical): Suffix `_fy`.
        - SHEAR (Horizontal): Suffixes `_fx` or `_fz`.
        - UNITS: Newtons.
        """

    # --- AGENT B: MUSCLE FORCES SPECIALIST ---
    elif target == 'forces':
        selected_df = dfs.get('forces')
        system_prefix = f"""
        You are the MUSCLE FORCE SPECIALIST.
        {global_rules}
        
        DOMAIN KNOWLEDGE (MUSCLES):
        - COLUMN: `Muscle_Name`
        - UNITS: Newtons.
        {muscle_mapping_info}
        """

    # --- AGENT C: ACTIVATIONS SPECIALIST ---
    elif target == 'activations':
        selected_df = dfs.get('activations')
        system_prefix = f"""
        You are the MUSCLE ACTIVATION SPECIALIST.
        {global_rules}
        
        DOMAIN KNOWLEDGE (ACTIVATION):
        - COLUMN: `Muscle_Name`
        - UNITS: 0.0 to 1.0 (Normalized Effort).
        {muscle_mapping_info}
        """

    # Safety Check
    if selected_df is None or selected_df.empty:
        return {"final_message": f"Error: Dataframe '{target}' is empty."}

    # ==================================================================================
    # STEP 3: EXECUTE
    # ==================================================================================
    try:
        agent = create_pandas_dataframe_agent(
            llm=llm,
            df=selected_df,
            verbose=True,
            allow_dangerous_code=True,
            agent_type="openai-tools",  
            handle_parsing_errors=True,
            prefix=system_prefix
        )

        # Force input context regarding df existence
        final_prompt = f"{instruction} (Recall: The dataframe `df` is already loaded. Use it.)"
        
        response = agent.invoke({"input": final_prompt})
        output_text = response["output"] if isinstance(response, dict) else str(response)

        return {"final_message": output_text}

    except Exception as e:
        return {"final_message": f"Agent Execution Error: {e}"}
    